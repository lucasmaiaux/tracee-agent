server:
  url: "wss://api.tracee.example.com/ws/agent"
  token: "votre-token-agent-ici"

capture:
  default_interface: "wlan0"   # surchargé par --interface
  snaplen: 512

logging:
  level: "INFO"                # DEBUG | INFO | WARNING | ERROR | CRITICAL
  file: "/var/log/tracee-agent.log"
